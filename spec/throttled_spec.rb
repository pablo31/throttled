require "spec_helper"

describe Throttled do
  it "has a version number" do
    expect(Throttled::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
